using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LRV : MonoBehaviour {

    public string LRVID;
    public float LRVCapacity;
    public int NoOfWagonsAttached;
    public bool moreWagonsNeeded;
    public float totalNoOfPassengers;
    public float percentageFull;
    public float speed;
    public float acceleration;
    public float deceleration;
	// Use this for initialization
	void Start () {
		        // initialise LRV capacity as per number of wagons

                //size of a wagon(make a variable or fixed?)

	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
